# 调度台看板 · <仓目录名> · 更新 <日期 时间>

调度台会话：<ListAgents 里的名字> · 窗口 workspace:<id>「调度台 #<n>」· context 到 30% 交派活权（见 SKILL.md handoff）
旧调度台还在收尾的票：<无 / 票号 + 在做什么>（新调度台等它报「收尾完」再关它窗口）
仓：<绝对路径> · 远端 <owner/repo> · 地图票 #<n>

## 给你看的（五行以内，纯人话，看完就够）

> 看板下面全是调度台自己的交接笔记（workspace 号、hash、未提交清单），用户不用往下看。
> 这一段只写：谁在等你、等你答什么、我建议怎么答。⛔ 不许出现 hash、会话 id、文件路径。

- #<N> <一句话说它在等什么> —— 我建议 <A>，因为 <一句理由>
- 没人等你的时候写「都在跑，没有要你答的」

## 在做

| 窗口（workspace:id） | 票 | 状态 | 动哪些目录 | 等谁 |
|---|---|---|---|---|
| workspace:23 | #146 取数层落点函数 | 开工 / 红阶段停 / 报完待核 / 审核中 / 待用户关 | 源表取数/ | 用户 / 审核代理 / 无 |

## 等用户答

- <一次一个问题；答了就删>

## 在跑的审核代理

- #<票> · 派出时间 · 结论回来后要做什么

## 下一批（frontier，等谁关了就开）

- #147 ← 等 #146 · #151 ← 等 #148

## 谁在动哪（防撞）

- 源表取数/ 五条链 source_*.py ← #146
- 界面/ ← #148（database_界面.py 除外 ← #149）

## 已关（本轮）

- #142 #143 #139 #145 已关并回填 #75

## 约定（本仓特有，别的仓照抄时改这里）

- 关票由用户说「关」；审核 Opus 级；提交按文件 add

窗口栏写成 `workspace:N (glm|kimi|opus|fable)`，模型按 SKILL.md「派票」的难度表选；没写模型的是 claude 默认模型。
