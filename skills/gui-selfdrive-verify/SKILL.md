---
name: gui-selfdrive-verify
description: 交付 GUI（tkinter/webview/Electron 等）前的自驱验证方法：agent 没法用鼠标点界面，"跑起来不报错"证明不了界面能用——写程序化驱动脚本逐状态断言。当写完或改完任何图形界面准备交付、要验证界面各状态（成功/失败/忙碌/空态）真的画对了、或用户说"验一下界面/UI 能不能用"时使用。测试替身与截图都会骗人，本 skill 给出不被骗的驱动与断言套路。
---

# GUI 自驱验证

agent 交付图形界面时点不了鼠标，光"启动不崩"和单测全绿都证明不了界面真能用——三次实证（tkinter 壳、webview 壳两轮）没有一次问题是靠通读代码或跑单测发现的。交付前给界面写一次性**驱动脚本**，程序化触发交互、逐状态断言画面，失败退出码非 0。

## 前提：入口必须可注入

UI 入口接收注入的引擎/数据源（`run(engine)` 模式），别在 UI 内部硬装配。驱动脚本用内存 fake 喂出**每一种**要验的状态：成功、各类失败、忙碌中、空列表、需联网……fake 能注错是关键——真环境很难按需出错。

## 驱动套路（按 UI 技术选）

**tkinter**：脚本自建 root、调应用的公开点击方法、循环 `root.update()` 推事件直到后台任务结束，断言状态栏/控件文案；中途可用 `screencapture -x` 截图人眼复核。**绝不能在 pytest 里进 mainloop**——会吊死测试进程，驱动脚本放 examples/ 手动跑。

**webview（pywebview 等）**：demo 脚本加 `--autopilot`：真开窗后在 `on_ready` 钩子里用 `evaluate_js` 逐条断言 DOM（元素数、文案、class、计算样式），走真 js_api 桥触发点击与轮询，断言失败退出码非 0。

## 会骗人的陷阱

1. **隐藏/遮挡窗口三部曲**（同一根因：页面不可见时浏览器降功耗）：①渲染帧冻结——截图是动画中途的陈旧帧，像"没渲染"的假 bug；②rAF 停摆——走 requestAnimationFrame 的挂载/回调压根不执行，querySelector 空手而归；③`setInterval` 被渐进节流——JS 侧定时采样只采到零星几个点。分辨法：读 `document.visibilityState` 与 `getComputedStyle`。对策：给页面留**不经 rAF 的同步挂载函数**；采样/轮询一律由 **Python 侧 `evaluate_js` 驱动**（驱动泵在页面外，不受页面节流管辖），JS 侧定时器只当装饰。
2. **绿色驱动脚本本身**：按"验证也要自证"的纪律，故意改错一条期望→确认脚本红→改回。没红过的断言不算断言。

## 报告

把每种状态的实际界面文案/断言结果贴进交付报告（不是"我验过了"一句话），退出码作为可复核的硬证据。
